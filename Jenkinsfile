pipeline {
    agent any

    stages {

        stage('Pull Code') {
            steps {
                echo 'Pulling code from GitHub...'
            }
        }

        stage('Check Files') {
            steps {
                sh 'ls -la'
            }
        }

    }
}
